/* ==========================================================
   admin-auth.js — Admin session guard
   True Jiu Jitsu Online

   Verifies the current user has the admin role via Netlify
   Identity. Redirects to login if not authenticated or
   if the role check fails. Loaded on every admin page.
   ========================================================== */

/* TODO: Phase 4 — implement at start of admin panel build */
